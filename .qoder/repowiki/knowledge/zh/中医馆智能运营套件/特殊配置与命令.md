- **一键初始化**：执行 `node _shared/scripts/auto-install.js --type tcm-clinic` 自动检测环境、安装 npm 依赖及 Playwright 浏览器。
- **设置向导**：在对话中触发“开始设置”，通过 5 步引导式问答自动生成 `_shared/config.json` 及各模块知识库。
- **环境自检**：运行 `node _shared/scripts/check-env.js` 诊断依赖、配置及知识库状态。