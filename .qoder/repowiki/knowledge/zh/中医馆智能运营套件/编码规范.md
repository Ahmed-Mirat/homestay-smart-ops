- 所有业务脚本均采用 CLI 模式设计，通过 `require.main === module` 判断是否直接运行，并支持 `module.exports` 供其他模块调用。
- 数据操作遵循“加载-修改-保存”模式，统一从 `_shared/data/` 读取 JSON 文件，内存中处理后同步写回，确保数据一致性。
- 外部依赖（如通知模块 `notifier.js`）的引入采用 try-catch 包裹，实现优雅降级，确保核心业务流程不因非关键服务失败而中断。
- 产品/实体查找优先使用唯一 ID 匹配，若使用名称匹配则强制校验唯一性，存在同名实体时抛出异常并要求用户指定 ID。